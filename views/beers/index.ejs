<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<main>
  
  <section>
    <h1>Boston Beer Hall</h1>
    <% if (user) { %>
      <h2>Add a Beer!</h2>
      <form action="/beers" method="POST">
        <label>
          Name: 
          <input type="text"  class="form-control" placeholder="Beer Name" name="name" autocomplete="off">
        </label>
        <label>
          Great?
          <input type="checkbox" name="great">
        </label>
        <button class="btn btn-warning btn-sm" type="submit">Add Beer</button>
      </form>
    <% } %>
  <h2>See the Beers Below!</h2>
	<ul>
  <% beers.forEach(beer => { %>
    <li><a href="/beers/<%= beer._id %>"><%= beer.name %></a> 
        is <%= beer.great ? 'great' : 'bad' %> 
    </li>
  <% }) %>
  </ul>
</section>
</main>

<%- include('../partials/footer') %>